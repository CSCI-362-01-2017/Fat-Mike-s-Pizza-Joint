name: successful test case for single line parsing
requirement: single line parsing
component:
    class: TestCase
    provider: TestCase
method: extract_contents
input:
    constructor: static
    method: "requirement: single line parsing"
oracle: ['requirement', 'single line parsing']
