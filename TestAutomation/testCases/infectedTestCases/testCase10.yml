name: no text error
requirement: single line parsing
component:
    class: TestCase
    provider: brokenPythonFiles/TestCase 
    method: extract_contents
input:
    constructor: static
    method: " "
oracle: IndexError
